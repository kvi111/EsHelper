<Page
    x:Class="esHelper.Index"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:esHelper"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Pivot Grid.Row="1" Name="pivot1">
            <PivotItem Header="Index" Name="pivotItemIndex">
                <Grid Height="AUTO" Width="AUTO">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="60"></RowDefinition>
                        <RowDefinition Height="60"></RowDefinition>
                        <RowDefinition Height="60"></RowDefinition>
                        <RowDefinition Height="*"></RowDefinition>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center" Text="Index list"/>
                    <StackPanel Grid.Column="0" Grid.Row="1" Orientation="Horizontal">
                        <!--<Border Background="AliceBlue">
                            <TextBlock>aaa</TextBlock>
                        </Border>-->
                        <AppBarButton Icon="Add" Label="add index" Click="AppBarButtonAdd_Click" IsCompact="True"/>
                        <AppBarButton Icon="Refresh" Label="refresh" Click="ToggleSwitch_Toggled" IsCompact="True"/>
                        <AppBarSeparator/>
                        <ToggleSwitch Name="ToggleSwitch1" IsOn="False" Toggled="ToggleSwitch_Toggled" HorizontalContentAlignment="Right"  HorizontalAlignment="Right">
                            <ToggleSwitch.OffContent>
                                <TextBlock Text="don't show sys index" />
                            </ToggleSwitch.OffContent>
                            <ToggleSwitch.OnContent>
                                <TextBlock Text="show sys index" />
                            </ToggleSwitch.OnContent>
                        </ToggleSwitch>
                    </StackPanel>
                    <StackPanel Grid.Column="0" Grid.Row="2" Orientation="Horizontal" Background="AliceBlue" Height="50" VerticalAlignment="Center">
                        <TextBlock Width="300" Text="Id"  VerticalAlignment="Center" TextAlignment="Center" />
                        <TextBlock Width="300" Text="Name" VerticalAlignment="Center" TextAlignment="Center" />
                        <TextBlock Width="150" Text="IsOpen" VerticalAlignment="Center"/>
                        <TextBlock Width="150" Text="Color" VerticalAlignment="Center"/>
                        <TextBlock Width="150" Text="Document Count" VerticalAlignment="Center" TextAlignment="Right"/>
                        <TextBlock Width="150" Text="Shards Count" VerticalAlignment="Center" TextAlignment="Right"/>
                        <TextBlock Width="150" Text="Data Space" VerticalAlignment="Center" TextAlignment="Right"/>
                        <TextBlock Width="200" Text="Action" VerticalAlignment="Center" TextAlignment="Center"/>
                    </StackPanel>
                    <ListView Name="listview1" SelectionMode="None" Grid.Column="0" Grid.Row="3" >
                        <!--<ListView.HeaderTemplate>
                            <DataTemplate>

                            </DataTemplate>
                        </ListView.HeaderTemplate>-->
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Height="40" VerticalAlignment="Center">
                                    <!--<TextBlock Width="300" Text="{Binding Id}"/>-->
                                    <TextBlock Width="300" Text="{Binding Id}" VerticalAlignment="Center" IsTextSelectionEnabled="True"/>
                                    <TextBlock Width="300" Text="{Binding Name}" VerticalAlignment="Center"/>
                                    <TextBlock Width="150" Text="{Binding isOpen}" VerticalAlignment="Center"/>
                                    <TextBlock Width="150" Text="{Binding Color}" VerticalAlignment="Center"/>
                                    <TextBlock Width="150" Text="{Binding DocumentCount}" VerticalAlignment="Center" TextAlignment="Right"/>
                                    <TextBlock Width="150" Text="{Binding ShardsCount}" VerticalAlignment="Center" TextAlignment="Right"/>
                                    <TextBlock Width="150" Text="{Binding DataSpace}" VerticalAlignment="Center" TextAlignment="Right"/>
                                    <HyperlinkButton VerticalAlignment="Center" Click="HyperlinkButtonBrowse_Click" Command="{Binding Id}" CommandParameter="{Binding Name}">browse</HyperlinkButton>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </PivotItem>
            <PivotItem Header="Search" Name="pivotItemSearch">
                <StackPanel Orientation="Vertical" Height="AUTO">
                    <StackPanel Orientation="Horizontal">
                        <AppBarButton Height="35" Width="35" Name="AppBarButtonFirst" Click="AppBarButtonFirst_Click">
                            <AppBarButton.Icon>
                                <SymbolIcon Margin="0,-10,0,0" Symbol="Previous"/>
                            </AppBarButton.Icon>
                            <ToolTipService.ToolTip>
                                <ToolTip Content="first page" Placement="Right"  HorizontalOffset="30" VerticalOffset="0" />
                            </ToolTipService.ToolTip>
                        </AppBarButton>
                        <AppBarButton Height="35" Width="35" Name="AppBarButtonPrevious" Click="AppBarButtonPrevious_Click">
                            <AppBarButton.Icon>
                                <SymbolIcon Margin="0,-10,0,0" Symbol="Back"/>
                            </AppBarButton.Icon>
                            <ToolTipService.ToolTip>
                                <ToolTip Content="previous page" Placement="Right"  HorizontalOffset="30" VerticalOffset="0" />
                            </ToolTipService.ToolTip>
                        </AppBarButton>
                        <TextBlock Height="35" Width="35" Name="textBlockPageIndex"/>
                        <AppBarButton Height="35" Width="35" Name="AppBarButtonNext" Click="AppBarButtonNext_Click">
                            <AppBarButton.Icon>
                                <SymbolIcon Margin="0,-10,0,0" Symbol="Forward"/>
                            </AppBarButton.Icon>
                            <ToolTipService.ToolTip>
                                <ToolTip Content="next page" Placement="Right"  HorizontalOffset="30" VerticalOffset="0" />
                            </ToolTipService.ToolTip>
                        </AppBarButton>
                        <AppBarButton Height="35" Width="35" Name="AppBarButtonLast" Click="AppBarButtonLast_Click">
                            <AppBarButton.Icon>
                                <SymbolIcon Margin="0,-10,0,0" Symbol="Next"/>
                            </AppBarButton.Icon>
                            <ToolTipService.ToolTip>
                                <ToolTip Content="last page" Placement="Right"  HorizontalOffset="30" VerticalOffset="0" />
                            </ToolTipService.ToolTip>
                        </AppBarButton>
                    </StackPanel>
                    <ScrollViewer Name="scrollView" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Visible" Height="AUTO">
                        <Grid Name="gridData">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="AUTO"></RowDefinition>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="AUTO"></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                        </Grid>
                    </ScrollViewer>
                </StackPanel>
            </PivotItem>
        </Pivot>
    </Grid>
</Page>
